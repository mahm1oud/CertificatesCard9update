# إعدادات بيئة الإنتاج - لاستخدامها عند نشر التطبيق على خادم خارجي
# تاريخ الإنشاء: 14/05/2025

NODE_ENV=production
PORT=5000

# إعدادات قاعدة البيانات PostgreSQL
# استخدم بيانات قاعدة البيانات الحقيقية على الخادم
DATABASE_URL=postgres://u240955251_colluser:700125733Mm@localhost:5432/u240955251_colliderdb
PGHOST=localhost
PGPORT=5432
PGUSER=u240955251_colluser
PGPASSWORD=700125733Mm
PGDATABASE=u240955251_colliderdb

# المسارات والمجلدات
UPLOAD_DIR=uploads
FONTS_DIR=fonts
API_URL=/api

# إعدادات الأمان
# يجب تغيير هذا المفتاح في بيئة الإنتاج!
SESSION_SECRET=prod_secret_key_change_this_in_production_70012573_v1

# إعدادات خاصة بالتطبيق
SITE_URL=https://your-domain.com
ADMIN_EMAIL=admin@your-domain.com

# إعدادات تسجيل الأخطاء والتتبع
LOG_LEVEL=error
ENABLE_ERROR_TRACKING=true
ERROR_LOG_FILE=logs/error.log

# مسار القوالب والشهادات الافتراضية
DEFAULT_TEMPLATES_DIR=static/templates
DEFAULT_CERTIFICATES_DIR=static/certificates

# لغة التطبيق الافتراضية
DEFAULT_LANGUAGE=ar

# إعدادات التحميل
MAX_UPLOAD_SIZE=10485760 # 10MB

# وقت انتهاء الجلسة (بالمللي ثانية) - 24 ساعة
SESSION_EXPIRY=86400000